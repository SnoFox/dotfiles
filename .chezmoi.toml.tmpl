{{- $snodots := ternary (get . "snodots") dict (hasKey . "snodots" ) -}}
{{- $isOkta := promptBoolOnce $snodots "isOkta" "Is this an Okta workstation?" false -}}

[data]
  snodots.isOkta = {{ $isOkta }}
